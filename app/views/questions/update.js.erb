document.querySelector('.question-errors').innerHTML = '<%= render 'shared/errors', resource: @question %>'

<% unless @question.errors.present? %>
  document.querySelector('.question').innerHTML = '<%= j render 'question_form', question: @question %>'
<% end %>
